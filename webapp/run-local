docker build -t webapp -f webapp/Dockerfile . 

docker run --rm -p 80:80 \                                                                                                                                                               ─╯
  --add-host host.docker.internal:host-gateway \
  -v "$PWD/webapp/nginx.local.conf:/etc/nginx/conf.d/default.conf:ro" \
  webapp